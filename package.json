{
  "name": "influx-metrics",
  "version": "0.0.1",
  "description": "Minimal wrapper around influx-client for TICK stack",
  "main": "./dist/metrics.js",
  "scripts": {
    "start": "tsc -p . && node ./dist/metrics.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/printzero/influx-metrics.git"
  },
  "author": "Ashish Shekar",
  "license": "ISC",
  "homepage": "https://github.com/printzero/influx-metrics/blob/master/README.md",
  "dependencies": {
    "influx": "^5.0.7"
  },
  "devDependencies": {
    "@types/node": "^11.10.4"
  }
}
